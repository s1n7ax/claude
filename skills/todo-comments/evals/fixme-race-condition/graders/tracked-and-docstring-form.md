---
type: llm
weight: 3
---

The FIXME is written in the language's doc-comment convention — a Python
docstring on `claim_next_job` — and is backed by an issue tracker: it carries a
GitHub / Jira / Linear issue URL, or the response created the issue, or the
response explicitly asked the user for the issue link / placed a
clearly-marked placeholder URL.

Fail this if a bare untracked `# FIXME` was left inline with no issue reference.
