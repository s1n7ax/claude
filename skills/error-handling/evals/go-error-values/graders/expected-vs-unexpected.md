---
type: llm
weight: 2
---

Expected failures (empty id, no such order) are distinguishable by the caller
from unexpected infrastructure failures (the query error) — they are not all
returned as the same undifferentiated error.
