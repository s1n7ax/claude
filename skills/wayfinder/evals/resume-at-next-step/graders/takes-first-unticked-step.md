---
type: llm
weight: 4
---

The response goes straight to the first unticked checklist line — "implement:
hotkey listener that toggles a recording flag" — and starts building it.

Fail this if it instead re-plans the map, re-opens settled requirements, asks the
user which step to work on, or starts with a different step such as the encoder
pipeline.
